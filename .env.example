# --- Servidor y Aplicación ---
NODE_ENV=development
PORT=8080

# --- Firebase ---
# El Project ID de tu proyecto en Firebase.
FIREBASE_PROJECT_ID=tu-id-de-proyecto-firebase

# ¡IMPORTANTE! Descarga tu Service Account Key JSON desde la consola de Firebase,
# guárdalo en tu proyecto (ej. en `src/config/serviceAccountKey.json`) y pon la ruta aquí.
# Es crucial para la autenticación del SDK de Admin en entornos de producción.
# Asegúrate de añadir el archivo JSON a tu .gitignore.
FIREBASE_SERVICE_ACCOUNT_PATH=
GOOGLE_APPLICATION_CREDENTIALS=

# --- Emuladores (para desarrollo local) ---
# Clave de API para el cliente web de Firebase. Para el emulador, puede ser cualquier string.
FIREBASE_WEB_API_KEY=test-api-key
# Host del emulador de autenticación de Firebase.
FIREBASE_AUTH_EMULATOR_HOST=localhost:9099


# --- Data Connect ---
# El ID del servicio DEBE coincidir con el 'serviceId' en `dataconnect.yaml`.
DATA_CONNECT_SERVICE_ID=service-id
# La región donde está desplegado tu servicio de Data Connect.
DATA_CONNECT_LOCATION=us-central1

# ¡CRUCIAL PARA LOCAL! Apunta el SDK de Admin al emulador de Data Connect.
# El puerto debe coincidir con el de tu `connector.yaml`.
DATA_CONNECT_EMULATOR_HOST=localhost:9399

# --- OpenAI ---
# Tu clave de API de OpenAI.
OPENAI_API_KEY=
# Modelo a usar para las llamadas a la API (ej. gpt-4o-mini).
OPENAI_MODEL=

# --- Configuración del Modelo LLM (Opcional) ---
# Puedes descomentar y ajustar estos valores para experimentar en tu .env.
# Si no están presentes, se usarán los valores por defecto definidos en `src/config/index.ts`.
# LLM_TEMPERATURE=0.7
# LLM_MAX_TOKENS=4000
# LLM_TOP_P=1
# LLM_FREQUENCY_PENALTY=0
# LLM_PRESENCE_PENALTY=0